# Optional: Change the upload directory
# UPLOAD_DIR=uploads

# Optional: Change the maximum file size (in bytes)
# MAX_FILE_SIZE=524288000

# Optional: Whisper model size (tiny, base, small, medium, large)
# WHISPER_MODEL=base

# Optional: Device for ML models (cuda or cpu)
# ML_DEVICE=cuda
